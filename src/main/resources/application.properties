# Nombre de la aplicación y puerto
spring.application.name=demo
server.port=8080

# Configuración de la base de datos (Render y GitHub Actions usan variables)
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/vote_fs3r}
spring.datasource.username=${DB_USER:vote_fs3r_user}
spring.datasource.password=${DB_PASSWORD:SVvs76gutB61D568xOPg3AAoiN8lin7q}

# Configuración JPA (Reactiva Hibernate y sincroniza estructura con la base)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect